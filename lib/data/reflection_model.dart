// lib/data/reflection_model.dart

/// Modelo básico que representa una reflexión.
/// Aunque no se usa intensamente ahora, prepara el código para APIs o DBs.
class Reflection {
  final String message;

  /// Constructor que requiere un mensaje no nulo.
  Reflection(this.message);
}
